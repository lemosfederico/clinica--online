import { DownloadExcelDirective } from './download-excel.directive';

describe('DownloadExcelDirective', () => {
  it('should create an instance', () => {
    const directive = new DownloadExcelDirective();
    expect(directive).toBeTruthy();
  });
});
