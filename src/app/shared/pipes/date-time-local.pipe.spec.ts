import { DateTimeLocalPipe } from './date-time-local.pipe';

describe('DateTimeLocalPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeLocalPipe();
    expect(pipe).toBeTruthy();
  });
});
