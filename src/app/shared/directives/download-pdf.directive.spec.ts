import { DownloadPdfDirective } from './download-pdf.directive';

describe('DownloadPdfDirective', () => {
  it('should create an instance', () => {
    const directive = new DownloadPdfDirective();
    expect(directive).toBeTruthy();
  });
});
